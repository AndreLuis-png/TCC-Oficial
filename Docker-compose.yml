services:
 api:
   build: .
   ports:
     - "5000:5000"
   depends_on:
     - db
 db:
   image: mysql:8.0
   environment:
     MYSQL_DATABASE: almoxarifado_db
     MYSQL_ROOT_PASSWORD: "mysql_root"
   ports:
     - "3306:3306"
   volumes:
     - ./dataB.sql:/docker-entrypoint-initdb.d/dataB.sql
